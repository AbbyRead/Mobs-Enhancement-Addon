package btw.community.abbyread.meap.extend;

public interface EntitySkeletonExtend {
    boolean meap$getIsBreakingTorch();
    void meap$setIsBreakingTorch(boolean isBreakingTorch);

    // ----------------------------------------------------------------
    // Getter / Setter for canXray
    // ----------------------------------------------------------------
    boolean meap$getCanXray();

    void meap$setCanXray(boolean value);
}
